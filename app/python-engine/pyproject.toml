[project]
name = "mint-lingo-engine"
version = "0.1.0"
description = "Local processing engine for Video Translator"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[build-system]
requires = ["setuptools>=77"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
